import styled from '@emotion/styled';

export const ListItem = styled.li`
  padding: 20px;
  box-shadow: 0 4px 8px rgba(90 0 90 / 20%);
  border-radius: 6px;
  border: 2px solid #7650e2;
  display: flex;
  flex-flow: column;
  gap: 8px;
`;

ListItem.displayName = 'BookListItem';
